# Ultra-minimal requirements for Vercel deployment
# Absolute essentials only to stay well under 250MB limit

# FastAPI core (minimal)
fastapi==0.104.1
uvicorn[standard]==0.24.0

# AI libraries (essential only)
openai==1.3.0

# Basic utilities
python-dotenv==1.0.0
pydantic==2.5.0
